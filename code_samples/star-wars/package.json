{
  "private": true,
  "name": "star-wars",
  "scripts": {
    "start": "browser-sync start --server public --files public",
    "deploy": "echo 'To setup deployment run: yarn setup-deploy'",
    "setup-deploy": "app-app --deploy && yarn deploy"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "stylelint": "^10.1.0",
    "stylelint-config-standard": "^18.3.0"
  }
}
